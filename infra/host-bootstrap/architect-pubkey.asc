-----BEGIN PGP PUBLIC KEY BLOCK-----
# PLACEHOLDER — exported by the architect ceremony at provisioning time
# (HSK §05). Replace this stub before the first production build with:
#   gpg --armor --export "Junior Thuram Nana <satoshinakamotobull@gmail.com>" \
#       > infra/host-bootstrap/architect-pubkey.asc
#
# The Worker.Dockerfile COPYs this file at build time so workers can
# verify dossier signatures against the architect's trust anchor without
# any network fetch. The private key never leaves the YubiKey.
-----END PGP PUBLIC KEY BLOCK-----
