# W-14 — Anti-hallucination L1-L12 not testable

| Field                                | Value                         |
| ------------------------------------ | ----------------------------- |
| ID                                   | W-14                          |
| Severity                             | **High**                      |
| Status                               | **🟩 committed (2026-05-17)** |
| Authoritative ref (in TRUTH or repo) | OPERATIONS.md §4 CI gates     |
| Source(s) in original docs           | SRD §20                       |

## Problem

SRD §20 enumerates twelve layers of anti-hallucination control but does not show their unit-test contracts. Each control is conceptual.

## Fix

**Write `prompts/synthetic-hallucinations.jsonl`** — 200 known-bad LLM outputs covering each layer's failure modes (fabricated citations, miscounted amounts, invented entities, schema violations, language drift, etc.). CI nightly + on PR (touching `packages/llm/`) asserts each layer rejects ≥ 95% of its corresponding category.

Corpus authored in M0c week 1, expanded with each new pattern, audit-trailed in `packages/llm-prompts/__tests__/`.

## Closure (2026-05-17, T4 of TODO.md sweep)

`packages/llm/__tests__/synthetic-hallucinations.jsonl` carries **224
rows** (`wc -l`), surpassing the 200-row target stated in the fix
section. The 12-layer guard stack is wired in
`packages/llm/src/guards.ts` (L1/L2/L3 schema-compliance,
citation-required, CID-in-context) and exercised end-to-end by the
SafeLlmRouter chokepoint per AI-SAFETY-DOCTRINE-v1. Per-pattern row
expansion can continue post-MVP without re-opening this weakness;
the regression contract is that the corpus only grows and per-layer
rejection ratios only tighten.

The PHASE-1-COMPLETION.md §A1 row had already marked this 🟩; the
INDEX row was the last stale carrier of the 🟧 marker. Brought back
into sync by T4 of the TODO.md sweep.
